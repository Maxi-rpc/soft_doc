# soft_doc
 soft template

# Instalar requirement.txt
pip install -m .\requirement.txt

# instalar SQL (se uso swamp)

db_config = {
    'host': 'localhost',
    'user': 'root',
    'database': 'db_python'
}